# Текст храним и выкладываем с LF: файлы каталога переводов и PHP не должны
# зависеть от того, на какой системе их клонировали.
* text=auto eol=lf

*.png  binary
*.jpg  binary
*.pdf  binary
*.mo   binary
